# This is a basic workflow to help you get started with Actions

name: Develop

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ develop ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  #workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Setting up .net core in vm
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 3.1

      # dotnet build and publish
      - name: Build with dotnet
        run: dotnet build --configuration Release
      - name: dotnet publish
        run: |
          dotnet publish -c Release -o dotnetcorewebapp
      - name: Download a single artifact
        uses: actions/download-artifact@v2
        with:
          name: my-artifact
          path: /home/runner/work/actionsdemo/actionsdemo/dotnetcorewebapp/
        env:
          DOTNET_PATH: /home/runner/work/actionsdemo/actionsdemo/dotnetcorewebapp/
      #- name: 'Run Azure webapp deploy action using publish profile credentials'
        #uses: azure/webapps-deploy@v1
        #with: 
          #app-name: dotnetcorewebapp19 # Replace with your app name
          #publish-profile: $ # Define secret variable in repository settings as per action documentation
          #package: './dotnetcorewebapp' 
